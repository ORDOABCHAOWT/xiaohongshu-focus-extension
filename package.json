{
  "name": "xiaohongshu-focus-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that replaces Xiaohongshu explore feed with a focused search page.",
  "scripts": {
    "check": "npm test && npm run validate && npm run check:agentic",
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-extension.js",
    "check:agentic": "node scripts/check-agentic-docs.js"
  }
}
